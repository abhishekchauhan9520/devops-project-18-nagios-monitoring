name: Validate Nagios project

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run shell validation
        run: bash tests/test_static.sh
      - name: Validate Compose file
        run: docker compose config
